# URL des FastAPI-Backends.
# Lokal: localhost. Produktion: als Environment-Variable in Vercel setzen
# (Project Settings -> Environment Variables), NICHT als Datei committen.
NEXT_PUBLIC_API_URL=http://localhost:8000
